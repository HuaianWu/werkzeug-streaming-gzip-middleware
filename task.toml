schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/werkzeug-streaming-gzip-middleware"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "kh2ce374ade2a068fafcae234785c5e1"
task_id = "werkzeug-streaming-gzip-middleware"
display_title = "Add streaming gzip response middleware"
display_description = "Negotiate gzip from Accept-Encoding with quality values and identity, stream large bodies, skip non-compressible responses, and maintain Content-Length, ETag, and Vary."
original_title = "流式 gzip 响应压缩中间件"
category = "feature_request"
language = "python"
repository_url = "https://github.com/pallets/werkzeug"
base_commit_hash = "0005c79e09bae5f4cc2bd8ccd468d7dafe24a455"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary 0005c79e09bae5f4cc2bd8ccd468d7dafe24a455 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:kh2ce374ade2a068fafcae234785c5e1-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
